version: "3.5"

networks:
  bp-internal-tools-network:
    name: bp-internal-tools-network

services:
  bp-internal-tools-app:
    restart: on-failure
    container_name: bp-internal-tools-app
    build:
      context: ../..
      dockerfile: ./boilerplate_django/docker/Dockerfile
      target: development
    env_file: ./.env
    command: gunicorn project.wsgi:application --bind 0.0.0.0:8000 --reload --threads=10
    ports:
      - "4321:8000"
    volumes:
      - ../src:/srv
      - ../../codi-cooperatiu-internal-tools:/codi-cooperatiu-internal-tools
    depends_on:
      - bp-internal-tools-db
    networks:
      - bp-internal-tools-network

  bp-internal-tools-db:
    container_name: bp-internal-tools-db
    image: postgres:14
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust
    volumes:
      - ../dumps:/dumps
    networks:
      - bp-internal-tools-network

  bp-internal-tools-selenium:
    container_name: bp-internal-tools-selenium
    image: selenium/standalone-chrome:4.16.0
    networks:
      - bp-internal-tools-network
